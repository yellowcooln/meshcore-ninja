source: github
repo: xJARiD/MeshCore-EastMesh
updatedAt: '2026-08-16T06:51:26.449Z'
releases:
  - version: repeater-bridge-espnow-v1.17.1
    name: Repeater ESP-NOW Bridge Firmware v1.17.1
    datetime: '2026-08-15T00:17:43Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/repeater-bridge-espnow-v1.17.1
    prerelease: false
    notes: "## MeshCore Repeater ESP-NOW Bridge\r\n\r\nRepeater ESP-NOW bridge firmware built from the upstream `meshcore-dev/MeshCore` firmware base.\r\n\r\nThis release updates the firmware base to upstream MeshCore `v1.17.1`.\r\n\r\n### What's Changed\r\n\r\n- Updated the firmware base to upstream MeshCore `v1.17.1`.\r\n- Improved reply routing: direct requests now reply over the stored return path when one is known, and replies fall back to the repeater's default scope when the request scope is unknown, instead of always flooding un-scoped.\r\n- Fixed the RX boosted gain setting reverting to the compiled-in default after an automatic gain control reset (SX126x radios).\r\n- Added `get radio.fem.txgain` and `set radio.fem.txgain on|off` for boards with a controllable external FEM PA gain (such as Station G3).\r\n- Included upstream board, radio, display, and platform fixes from MeshCore dev.\r\n\r\n### Breaking Changes\r\n\r\n- None in 1.17.1. (If upgrading from 1.16.x or earlier: saved settings move to the new `prefs.json` format on first boot, and downgrading afterwards reverts the device to the old pre-migration settings file.)\r\n\r\n### Flashing\r\n\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.8.2
    name: Observer EastMesh Firmware v1.17.1-eastmesh-v2026.8.2
    datetime: '2026-08-15T00:19:49Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.8.2
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.17.1` (as of 2026-08-15).\r\n\r\nThis release adds a **Wi-Fi gateway connectivity watchdog** — observers that sit \"connected\" to an access point that has lost its uplink now detect the dead gateway and reconnect on their own — plus a manual `wifi reconnect` command, and updates the firmware base to **upstream MeshCore v1.17.1**.\r\n\r\n### What's Changed\r\n\r\n- **New: gateway connectivity watchdog.** While connected, the node ARP-probes its gateway every 30 seconds. If the gateway stays silent for 3 minutes (for example, an AP that keeps beaconing after losing its wired uplink), the node forces a full disconnect and rescan on its own so it can roam to a healthy AP, backing off up to 48 minutes between attempts while the outage persists.\r\n- Added `wifi reconnect` to drop the current association and rejoin from a full channel scan — for nodes that report connected but are unreachable over the network.\r\n- Extended `get wifi.status` with gateway health (`gw:ok|lost`) and the watchdog reconnect count (`wd:<n>`), also shown in the web panel.\r\n- Merged upstream MeshCore `v1.17.1`, including improved reply routing (direct requests reply over known return paths, and replies fall back to the default scope when the request scope is unknown), LR2021 radio improvements, and board/platform fixes.\r\n- Fixed the RX boosted gain setting reverting to the compiled-in default after an automatic gain control reset (SX126x radios).\r\n- Adopted upstream's SH1106 display address fallback (0x3C/0x3D) so board revisions with the other SA0 strap setting still get a working display, while keeping the EastMesh T-Beam S3 Supreme bring-up order and headless fallback.\r\n\r\n### Breaking Changes\r\n\r\n- None in 2026.8.2. (If upgrading from 2026.7.x or earlier: saved settings move to the new `prefs.json` format on first boot, and downgrading afterwards reverts the device to the old pre-migration settings file.)\r\n\r\n### Notes\r\n\r\n- **If your observer has ever sat \"connected\" with good signal but unreachable on the network until someone rebooted it, this release is the fix** — the watchdog now detects the dead gateway and reconnects automatically. `wd:<n>` in `get wifi.status` counts these forced reconnects since boot.\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` and `waev` are available as global second brokers.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, `set mqtt.waev on`, etc.). A maximum of two brokers can be enabled at once.\r\n- `waev` (`mqtt.waev.app`) uses a 1-hour auth token that the firmware refreshes automatically, so no extra configuration is needed. Set your Waev account email with `set mqtt.email <email>` so Waev can link the observer to your account.\r\n- MQTT status messages report cumulative `packets_sent` and `packets_received` alongside the existing radio stats; both counters reset on reboot.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments and uses username/password auth only. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert pac\n…"
  - version: observer-eastmesh-bridge-espnow-v2026.8.2
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.17.1-eastmesh-v2026.8.2
    datetime: '2026-08-15T00:19:29Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.8.2
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.17.1` (as of 2026-08-15).\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.8.2` — the new Wi-Fi gateway connectivity watchdog, the `wifi reconnect` command, and the upstream MeshCore v1.17.1 base — with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.8.2`:\r\n  - **New: gateway connectivity watchdog.** While connected, the node ARP-probes its gateway every 30 seconds. If the gateway stays silent for 3 minutes (for example, an AP that keeps beaconing after losing its wired uplink), the node forces a full disconnect and rescan on its own so it can roam to a healthy AP, backing off up to 48 minutes between attempts while the outage persists.\r\n  - Added `wifi reconnect` to drop the current association and rejoin from a full channel scan — for nodes that report connected but are unreachable over the network.\r\n  - Extended `get wifi.status` with gateway health (`gw:ok|lost`) and the watchdog reconnect count (`wd:<n>`), also shown in the web panel.\r\n  - Merged upstream MeshCore `v1.17.1`, including improved reply routing (direct requests reply over known return paths, and replies fall back to the default scope when the request scope is unknown), LR2021 radio improvements, and board/platform fixes.\r\n  - Fixed the RX boosted gain setting reverting to the compiled-in default after an automatic gain control reset (SX126x radios).\r\n  - Adopted upstream's SH1106 display address fallback (0x3C/0x3D) so board revisions with the other SA0 strap setting still get a working display, while keeping the EastMesh T-Beam S3 Supreme bring-up order and headless fallback.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- None in 2026.8.2. (If upgrading from 2026.7.x or earlier: saved settings move to the new `prefs.json` format on first boot, and downgrading afterwards reverts the device to the old pre-migration settings file.)\r\n\r\n### Notes\r\n\r\n- **If your observer has ever sat \"connected\" with good signal but unreachable on the network until someone rebooted it, this release is the fix** — the watchdog now detects the dead gateway and reconnects automatically. `wd:<n>` in `get wifi.status` counts these forced reconnects since boot.\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: companion-wifi-v1.17.1
    name: Companion WiFi Firmware v1.17.1
    datetime: '2026-08-15T00:16:59Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/companion-wifi-v1.17.1
    prerelease: false
    notes: "## MeshCore EastMesh Companion WiFi\r\n\r\nThis is standard MeshCore companion WiFi firmware based on MeshCore `v1.17.1`, with EastMesh additions for Wi-Fi setup and recovery.\r\n\r\n### What's Changed in 1.17.1\r\n\r\n- Updated the firmware base to upstream MeshCore `v1.17.1`.\r\n- Fixed the RX boosted gain setting reverting to the compiled-in default after an automatic gain control reset (SX126x radios) — the value you set now sticks.\r\n- Included upstream board, radio, display, and platform fixes from MeshCore dev, including external FEM gain groundwork for companion builds.\r\n\r\n### What's Different from Stock MeshCore\r\n\r\n**Recovery access point.** If the device has no Wi-Fi credentials, or can't connect to its network for 60 seconds, it broadcasts its own `EastMesh-WiFi` access point so you can fix the Wi-Fi settings in place — no serial cable needed. The AP shuts down automatically once the device connects to its configured network.\r\n\r\n- The AP password is the device pin, zero-padded to 8 digits. Devices with a screen show the pin on the home screen (`Pin:NNNNNN` → password `00NNNNNN`); headless devices default to pin `123456` (password `00123456`); `set pin <pin>` overrides both.\r\n- Once connected to the AP, the rescue CLI is available over TCP: `telnet 192.168.4.1` (or `nc 192.168.4.1 23`), then `set wifi.ssid <ssid>`, `set wifi.pwd <password>`, `reboot`.\r\n- The companion app works over the recovery AP too: connect to host `192.168.4.1` port `5000`.\r\n\r\nThe EastMesh Wi-Fi CLI commands, available over serial rescue and the recovery AP:\r\n\r\n- `get wifi.status`\r\n- `get wifi.ssid`\r\n- `get wifi.powersaving`\r\n- `set wifi.ssid <ssid>`\r\n- `set wifi.pwd <password>`\r\n- `set wifi.powersaving none|min|max`\r\n\r\n### Breaking Changes\r\n\r\n- None in 1.17.1. (If upgrading from 1.16.x or earlier: saved settings move to the new `prefs.json` format on first boot, and downgrading afterwards reverts the device to the old pre-migration settings file.)\r\n\r\n### Notes\r\n\r\n- Headless devices in public areas should set a custom pin (`set pin <pin>`) — the default recovery AP password `00123456` is documented, so treat it like a default router password\r\n- Everything else behaves like the normal MeshCore companion WiFi firmware\r\n- Use the file that matches your board exactly\r\n- Companion WiFi releases use the upstream MeshCore version number\r\n\r\n### Documentation\r\n\r\nhttps://xjarid.github.io/MeshCore-EastMesh/\r\n\r\n### Flashing\r\n\r\n- https://flasher.eastmesh.au/\r\n- https://flasher.meshcore.io/\r\n\r\n### Assets\r\n\r\n- `.bin` – standard update\r\n- `-merged.bin` – full image (after erase, ESP32)"
  - version: observer-eastmesh-bridge-mqtt-v2026.8.2
    name: Observer EastMesh Firmware with MQTT Bridge v1.17.1-eastmesh-v2026.8.2
    datetime: '2026-08-16T06:02:56Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-mqtt-v2026.8.2
    prerelease: true
  - version: repeater-bridge-espnow-v1.17.0
    name: Repeater ESP-NOW Bridge Firmware v1.17.0
    datetime: '2026-08-09T08:11:48Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/repeater-bridge-espnow-v1.17.0
    prerelease: false
    notes: "## MeshCore Repeater ESP-NOW Bridge\r\n\r\nRepeater ESP-NOW bridge firmware built from the upstream `meshcore-dev/MeshCore` firmware base.\r\n\r\nThis release updates the firmware base to upstream MeshCore `v1.17.0`.\r\n\r\n### What's Changed\r\n\r\n- Updated the firmware base to upstream MeshCore `v1.17.0`.\r\n- Moved saved node settings to upstream's new key-based `prefs.json` format; existing settings, including bridge configuration, are migrated automatically on first boot.\r\n- Included upstream board, radio, display, and platform fixes from MeshCore dev, including new board support and an external watchdog manager.\r\n\r\n### Breaking Changes\r\n\r\n- Saved settings move to the new `prefs.json` format on first boot; downgrading to earlier firmware afterwards reverts the device to the old (pre-migration) settings file.\r\n\r\n### Flashing\r\n\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.8.1
    name: Observer EastMesh Firmware v1.17.0-eastmesh-v2026.8.1
    datetime: '2026-08-09T09:21:43Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.8.1
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.17.0` (as of 2026-08-09).\r\n\r\nThis release updates the firmware base to **upstream MeshCore v1.17.0**, moves saved settings to upstream's new key-based `prefs.json` format — **with automatic migration, so your existing settings carry over on first boot** — and **fixes the web panel becoming unreachable after long uptimes** (`PR_CONNECT_RESET_ERROR` in the browser until reboot).\r\n\r\n### What's Changed\r\n\r\n- Merged upstream MeshCore `v1.17.0`, including new board support, ethernet interface groundwork, LR2021 radio support, display and UI improvements, and board/platform fixes.\r\n- Moved saved node settings to upstream's new key-based `prefs.json` format. Existing settings — including EastMesh fan control and bridge peer configuration — are migrated automatically on first boot; no reconfiguration needed this time.\r\n- Fixed the web panel refusing connections (browser `PR_CONNECT_RESET_ERROR`) after long uptimes: stale keep-alive connections from vanished clients are now evicted instead of permanently occupying the connection pool.\r\n- Hardened T-Beam S3 Supreme display bring-up: restored the proven SH1106 init order so the OLED reliably comes up at boot, and a failed or absent display now boots the node headless instead of crash-looping.\r\n\r\n### Breaking Changes\r\n\r\n- Saved settings move to the new `prefs.json` format on first boot. Upgrading is seamless, but downgrading to earlier firmware afterwards reverts the device to the old (pre-migration) settings file — anything changed since the upgrade would be lost.\r\n\r\n### Notes\r\n\r\n- **If your observer's web panel stopped responding after a week or more of uptime (connection reset in the browser), this release is the fix** — previously only a reboot would recover it.\r\n- Settings migrate automatically this time — unlike 2026.7.0, there is nothing to re-set after updating.\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` and `waev` are available as global second brokers.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, `set mqtt.waev on`, etc.). A maximum of two brokers can be enabled at once.\r\n- `waev` (`mqtt.waev.app`) uses a 1-hour auth token that the firmware refreshes automatically, so no extra configuration is needed. Set your Waev account email with `set mqtt.email <email>` so Waev can link the observer to your account.\r\n- MQTT status messages report cumulative `packets_sent` and `packets_received` alongside the existing radio stats; both counters reset on reboot.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments and uses username/password auth only. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.8.1
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.17.0-eastmesh-v2026.8.1
    datetime: '2026-08-09T09:21:29Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.8.1
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.17.0` (as of 2026-08-09).\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.8.1` — the upstream MeshCore v1.17.0 base, the automatic migration to the new key-based `prefs.json` settings format, the web panel long-uptime fix, and the T-Beam S3 Supreme display bring-up fix — with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.8.1`:\r\n  - Merged upstream MeshCore `v1.17.0`, including new board support, ethernet interface groundwork, LR2021 radio support, display and UI improvements, and board/platform fixes.\r\n  - Moved saved node settings to upstream's new key-based `prefs.json` format. Existing settings — including bridge peer and fan control — are migrated automatically on first boot; no reconfiguration needed this time.\r\n  - Fixed the web panel refusing connections (browser `PR_CONNECT_RESET_ERROR`) after long uptimes.\r\n  - Hardened T-Beam S3 Supreme display bring-up so a failed boot-time OLED probe boots the node headless instead of crash-looping.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- Saved settings move to the new `prefs.json` format on first boot. Upgrading is seamless, but downgrading to earlier firmware afterwards reverts the device to the old (pre-migration) settings file — anything changed since the upgrade would be lost.\r\n\r\n### Notes\r\n\r\n- **If your observer's web panel stopped responding after a week or more of uptime (connection reset in the browser), this release is the fix** — previously only a reboot would recover it.\r\n- Settings migrate automatically this time — unlike 2026.7.0, there is nothing to re-set after updating; bridge peer and channel settings carry over.\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: companion-wifi-v1.17.0
    name: Companion WiFi Firmware v1.17.0
    datetime: '2026-08-09T09:21:19Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/companion-wifi-v1.17.0
    prerelease: false
    notes: "## MeshCore EastMesh Companion WiFi\r\n\r\nThis is standard MeshCore companion WiFi firmware based on MeshCore `v1.17.0`, with EastMesh additions for Wi-Fi setup and recovery.\r\n\r\n### What's Changed in 1.17.0\r\n\r\n- Updated the firmware base to upstream MeshCore `v1.17.0`, including the new interface manager for BLE, Wi-Fi, USB, and serial connections.\r\n- Saved settings move to upstream's new key-based `prefs.json` format; existing settings, including saved Wi-Fi credentials, are migrated automatically on first boot.\r\n- **New: recovery access point.** If the device has no Wi-Fi credentials, or can't connect to its network for 60 seconds, it broadcasts its own `EastMesh-WiFi` access point so you can fix the Wi-Fi settings in place — no serial cable needed. The AP shuts down automatically once the device connects to its configured network.\r\n  - The AP password is the device pin, zero-padded to 8 digits. Devices with a screen show the pin on the home screen (`Pin:NNNNNN` → password `00NNNNNN`); headless devices default to pin `123456` (password `00123456`); `set pin <pin>` overrides both.\r\n  - Once connected to the AP, the rescue CLI is available over TCP: `telnet 192.168.4.1` (or `nc 192.168.4.1 23`), then `set wifi.ssid <ssid>`, `set wifi.pwd <password>`, `reboot`.\r\n\r\n### What's Different from Stock MeshCore\r\n\r\nThe EastMesh Wi-Fi CLI commands, available over serial rescue and the recovery AP:\r\n\r\n- `get wifi.status`\r\n- `get wifi.ssid`\r\n- `get wifi.powersaving`\r\n- `set wifi.ssid <ssid>`\r\n- `set wifi.pwd <password>`\r\n- `set wifi.powersaving none|min|max`\r\n\r\nPlus the recovery access point described above.\r\n\r\n### Breaking Changes\r\n\r\n- Saved settings move to the new `prefs.json` format on first boot; downgrading to earlier firmware afterwards reverts the device to the old (pre-migration) settings file.\r\n- Headless devices in public areas should set a custom pin (`set pin <pin>`) — the default recovery AP password `00123456` is documented, so treat it like a default router password.\r\n\r\n### Notes\r\n\r\n- Everything else behaves like the normal MeshCore companion WiFi firmware\r\n- Use the file that matches your board exactly\r\n- Companion WiFi releases use the upstream MeshCore version number\r\n\r\n### Documentation\r\n\r\nhttps://xjarid.github.io/MeshCore-EastMesh/\r\n\r\n### Flashing\r\n\r\n- https://flasher.eastmesh.au/\r\n- https://flasher.meshcore.io/\r\n\r\n### Assets\r\n\r\n- `.bin` – standard update\r\n- `-merged.bin` – full image (after erase, ESP32)"
  - version: observer-eastmesh-v2026.7.0
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.7.0
    datetime: '2026-07-19T04:09:45Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.7.0
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0` plus upstream dev updates (as of 2026-07-09).\r\n\r\nThis release syncs with **upstream MeshCore dev** — bringing power-off/deep-sleep improvements, SX1262 LDO support, hardware channel-activity detection, and board/platform fixes — **realigns saved settings storage** with upstream so future syncs can't shift or corrupt EastMesh settings, and **restores EastMesh/MeshMapper broker TLS trust** after a server-side certificate change.\r\n\r\n### What's Changed\r\n\r\n- Merged upstream MeshCore dev updates including power-off/deep-sleep improvements, SX1262 LDO support, hardware channel-activity detection, radio gain fixes, and board/platform fixes.\r\n- Realigned saved node settings to upstream byte offsets and moved EastMesh-specific fields behind a reserved gap so future upstream syncs no longer shift or corrupt them.\r\n- Restored EastMesh and MeshMapper broker TLS trust after `mqtt2.eastmesh.au` moved from Let's Encrypt to a Google Trust Services certificate.\r\n- Bumped CI build and docs workflows to Python 3.13.\r\n\r\n### Breaking Changes\r\n\r\n- The saved settings layout changed: devices upgrading from `2026.6.6` or earlier may need EastMesh-specific settings (such as fan control and bridge peer) and recently added radio settings reconfigured after the update.\r\n\r\n### Notes\r\n\r\n- **If your observer stopped uplinking to `eastmesh-au` or `meshmapper` with TLS certificate errors, this release is the fix** — update and the connection will recover on its own.\r\n- After updating, check your fan control, bridge peer, and recently added radio settings (see Breaking Changes) and re-set any that came back at defaults.\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` and `waev` are available as global second brokers.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, `set mqtt.waev on`, etc.). A maximum of two brokers can be enabled at once.\r\n- `waev` (`mqtt.waev.app`) uses a 1-hour auth token that the firmware refreshes automatically, so no extra configuration is needed. Set your Waev account email with `set mqtt.email <email>` so Waev can link the observer to your account.\r\n- MQTT status messages report cumulative `packets_sent` and `packets_received` alongside the existing radio stats; both counters reset on reboot.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments and uses username/password auth only. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.7.0
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.16.0-eastmesh-v2026.7.0
    datetime: '2026-07-19T04:09:38Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.7.0
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.16.0` plus upstream dev updates (as of 2026-07-09).\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.7.0` — the upstream MeshCore dev sync, the saved settings storage realignment, and the EastMesh/MeshMapper broker TLS fix — with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.7.0`.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- The saved settings layout changed: devices upgrading from `2026.6.6` or earlier may need EastMesh-specific settings (such as fan control and bridge peer) and recently added radio settings reconfigured after the update.\r\n\r\n### Notes\r\n\r\n- **If your observer stopped uplinking to `eastmesh-au` or `meshmapper` with TLS certificate errors, this release is the fix** — update and the connection will recover on its own.\r\n- After updating, check your bridge peer, fan control, and recently added radio settings (see Breaking Changes) and re-set any that came back at defaults.\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.6.6
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.6.6
    datetime: '2026-06-27T13:12:29Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.6.6
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release adds **packet counters** to the MQTT status payload — each observer now reports cumulative `packets_sent` and `packets_received` totals in its `status` message — so brokers and dashboards such as Waev and MeshMapper can track per-node throughput.\r\n\r\n### What's Changed\r\n\r\n- Added `packets_sent` and `packets_received` counters to the MQTT `status` payload `stats` object, reporting cumulative radio TX/RX packet totals to align with the Waev/MeshMapper status schema.\r\n\r\n### Breaking Changes\r\n\r\n- None in this release.\r\n\r\n### Notes\r\n\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` and `waev` are available as global second brokers.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, `set mqtt.waev on`, etc.). A maximum of two brokers can be enabled at once.\r\n- `waev` (`mqtt.waev.app`) uses a 1-hour auth token that the firmware refreshes automatically, so no extra configuration is needed. Set your Waev account email with `set mqtt.email <email>` so Waev can link the observer to your account.\r\n- MQTT status messages now report cumulative `packets_sent` and `packets_received` alongside the existing radio stats; both counters reset on reboot.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments and uses username/password auth only. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.6.6
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.16.0-eastmesh-v2026.6.6
    datetime: '2026-06-27T13:06:57Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.6.6
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.6.6`, with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.6.6`.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- `get mqtt.status` output changed from per-broker fields to `p:`/`s:` broker slots; update anything that parses the previous format.\r\n- Saved LetsMesh EU/US broker selections are cleared once on upgrade; re-enable a broker such as MeshMapper if you need a second uplink.\r\n\r\n### Notes\r\n\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.6.5
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.6.5
    datetime: '2026-06-26T02:52:13Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.6.5
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release adds the **Waev** curated broker as a selectable MQTT uplink — with automatic short-lived auth tokens to satisfy Waev's requirements — and widens the Custom MQTT settings panel in the web panel.\r\n\r\n### What's Changed\r\n\r\n- Added the `waev` curated broker (`wss://mqtt.waev.app:443/mqtt`) using WSS, verified TLS, and MeshCore JWT authentication with the broker host as the token audience.\r\n- Added `get mqtt.waev` and `set mqtt.waev on|off` to toggle the Waev broker, and added Waev to the **Primary MQTT** and **Secondary MQTT** dropdowns in the web panel.\r\n- Added per-broker JWT auth token lifetimes; Waev receives a 1-hour token to match its shorter maximum token lifetime, while the other brokers keep the existing lifetime.\r\n- Fixed the web panel so the **Custom MQTT settings** panel spans the full width of the MQTT Servers card when Custom is selected.\r\n- Updated the README and custom CLI guides for the Waev broker.\r\n\r\n### Breaking Changes\r\n\r\n- None in this release.\r\n\r\n> If you are upgrading from before `2026.6.4`, that release's changes still apply: `get mqtt.status` switched from per-broker fields to `p:`/`s:` broker slots, and saved LetsMesh EU/US selections are cleared once on upgrade.\r\n\r\n### Notes\r\n\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` and `waev` are available as global second brokers.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, `set mqtt.waev on`, etc.). A maximum of two brokers can be enabled at once.\r\n- `waev` (`mqtt.waev.app`) uses a 1-hour auth token that the firmware refreshes automatically, so no extra configuration is needed. Set your Waev account email with `set mqtt.email <email>` so Waev can link the observer to your account.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments and uses username/password auth only. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.6.5
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.16.0-eastmesh-v2026.6.5
    datetime: '2026-06-26T02:50:06Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.6.5
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.6.5`, with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.6.5`.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- `get mqtt.status` output changed from per-broker fields to `p:`/`s:` broker slots; update anything that parses the previous format.\r\n- Saved LetsMesh EU/US broker selections are cleared once on upgrade; re-enable a broker such as MeshMapper if you need a second uplink.\r\n\r\n### Notes\r\n\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.6.4
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.6.4
    datetime: '2026-06-23T00:27:09Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.6.4
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release replaces the retired LetsMesh brokers with the new MeshMapper broker and reworks broker selection into Primary and Secondary slots.\r\n\r\n### What's Changed\r\n\r\n- Added the `meshmapper` curated broker (`wss://mqtt.meshmapper.net:443/mqtt`) using WSS, verified TLS, and MeshCore JWT authentication with the broker host as the token audience.\r\n- Added `get mqtt.meshmapper` and `set mqtt.meshmapper on|off` to toggle the MeshMapper broker.\r\n- Reworked the web-panel broker section into **Primary MQTT** and **Secondary MQTT** dropdowns that enforce the two-broker maximum and reveal the custom host, username, and password fields when Custom is selected.\r\n- Retired the unmaintained LetsMesh EU/US brokers; they remain selectable but are marked retired, and saved selections are cleared once on upgrade so dead brokers stop retrying.\r\n- Shortened `get mqtt.status` to report the two active broker slots as `p:<broker>:<state>` and `s:<broker>:<state>` (for example `p:meshmapper:up`), keeping the reply within the LoRa text limit.\r\n- Custom MQTT remains username/password only; no JWT/web auth is used for custom brokers.\r\n- Updated the README, web panel, and custom CLI guides for MeshMapper and the Primary/Secondary broker layout.\r\n\r\n### Breaking Changes\r\n\r\n- `get mqtt.status` output changed from per-broker fields to `p:`/`s:` broker slots; update anything that parses the previous format.\r\n- Saved LetsMesh EU/US broker selections are cleared once on upgrade; re-enable a broker such as MeshMapper if you need a second uplink.\r\n\r\n### Notes\r\n\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured; `meshmapper` is available as a global second broker.\r\n- Select brokers from the Primary and Secondary dropdowns in the web panel, or toggle them from the CLI (`set mqtt.eastmesh-au on`, `set mqtt.meshmapper on`, etc.). A maximum of two brokers can be enabled at once.\r\n- LetsMesh EU/US are retired but still selectable for legacy use.\r\n- Custom MQTT is available for private broker deployments. For TCP custom MQTT, use the broker host and port as before. For WSS custom MQTT, set the host and port, select WSS transport, and the firmware connects using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.6.4
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.16.0-eastmesh-v2026.6.4
    datetime: '2026-06-23T00:26:58Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.6.4
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.6.4`, with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.6.4`.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\n- `get mqtt.status` output changed from per-broker fields to `p:`/`s:` broker slots; update anything that parses the previous format.\r\n- Saved LetsMesh EU/US broker selections are cleared once on upgrade; re-enable a broker such as MeshMapper if you need a second uplink.\r\n\r\n### Notes\r\n\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.6.3
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.6.3
    datetime: '2026-06-09T10:19:45Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.6.3
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release adds secure WebSocket transport support for custom MQTT brokers from the Observer web panel and CLI.\r\n\r\n### What's Changed\r\n\r\n- Added TCP/WSS transport selection for custom MQTT brokers.\r\n- Added web-panel Custom MQTT transport control for switching between TCP and WSS.\r\n- Added CLI support for viewing and setting the custom MQTT transport.\r\n- Kept custom MQTT authentication to username and password only; no JWT/web auth is used for custom brokers.\r\n- Verified custom MQTT WSS connections with the ESP-IDF x509 root CA bundle.\r\n- Fixed custom MQTT WSS to use the `/mqtt` WebSocket path.\r\n- Configured custom MQTT WSS using a full `wss://host:port/mqtt` URI so ESP-MQTT handles the WebSocket connection consistently.\r\n- Updated `get mqtt.status` to show the custom MQTT transport and state, for example `custom:wss:up`.\r\n\r\n### Breaking Changes\r\n\r\nNone.\r\n\r\n### Notes\r\n\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured.\r\n- Custom MQTT is available for private broker deployments.\r\n- For TCP custom MQTT, use the broker host and port as before.\r\n- For WSS custom MQTT, set the host and port, select WSS transport, and the firmware will connect using `wss://host:port/mqtt`.\r\n- WSS uses TLS encryption and validates against the ESP-IDF x509 root CA bundle.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-bridge-espnow-v2026.6.3
    name: Observer EastMesh Firmware with ESP-NOW Bridge v1.16.0-eastmesh-v2026.6.3
    datetime: '2026-06-09T10:18:22Z'
    url: >-
      https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-bridge-espnow-v2026.6.3
    prerelease: false
    notes: "## MeshCore EastMesh Observer with ESP-NOW Bridge\r\n\r\nEastMesh `observer-eastmesh-bridge-espnow` firmware for MeshCore boards that combine secure MQTT uplink with ESP-NOW bridge support, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release includes the same Observer updates as `observer-eastmesh-v2026.6.3`, with ESP-NOW bridge support included.\r\n\r\n### What's Changed\r\n\r\n- Same changes as `observer-eastmesh-v2026.6.3`.\r\n- Includes ESP-NOW bridge firmware targets using the `*_repeater_observer_espnow` PlatformIO environment suffix.\r\n\r\n### Breaking Changes\r\n\r\nNone.\r\n\r\n### Notes\r\n\r\n- This track is separate from `observer-eastmesh`.\r\n- Use `get wifi.status` to confirm the active 2.4 GHz Wi-Fi channel before setting `bridge.channel`.\r\n- `bridge.channel` should match the connected Wi-Fi channel for reliable ESP-NOW bridge operation.\r\n- For maximum heap headroom after setup, disable the web panel with `set web off`.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
  - version: observer-eastmesh-v2026.6.2
    name: Observer EastMesh Firmware v1.16.0-eastmesh-v2026.6.2
    datetime: '2026-06-07T05:03:35Z'
    url: https://github.com/xJARiD/MeshCore-EastMesh/releases/tag/observer-eastmesh-v2026.6.2
    prerelease: false
    notes: "## MeshCore EastMesh Observer\r\n\r\nEastMesh `observer-eastmesh` firmware for MeshCore boards with secure MQTT uplink and optional local web management, based on MeshCore firmware `v1.16.0`.\r\n\r\nThis release improves Observer boot reliability, T-Beam S3 Supreme telemetry, Wi-Fi/OTA persistence, and web-panel archive maintenance.\r\n\r\n### What's Changed\r\n\r\n- Fixed LilyGo T-Beam S3 Supreme BME280 detection and live telemetry.\r\n- Added board-specific I2C recovery for T-Beam S3 Supreme BME280 reads.\r\n- Changed live `/stats` sensor values to use the main-loop sensor snapshot.\r\n- Improved boot-time Wi-Fi startup on T-Beam S3 Supreme.\r\n- Added last-connected Wi-Fi channel learning with fallback to full scan.\r\n- Added NVS-backed Wi-Fi preference recovery so SSID and password survive OTA updates even if filesystem prefs are lost.\r\n- Hardened Wi-Fi retry handling so scan-mode connects are not interrupted early.\r\n- Fixed `set wifi.powersaving none` when power saving was already disabled.\r\n- Bounded archive neighbour restore and history reads to avoid slow boots from large SD-card archive files.\r\n- Added web-panel and CLI SD purge actions for supported SD-card targets.\r\n- Hardened OTA startup by binding before reporting success and falling back to alternate ports if port 80 is still busy.\r\n\r\n### Breaking Changes\r\n\r\nNone.\r\n\r\n### Notes\r\n\r\n- `eastmesh-au` remains the recommended MQTT endpoint once `mqtt.iata` is configured.\r\n- Custom MQTT is available for private broker deployments.\r\n- For maximum heap headroom on busy observers, use the web panel for setup and troubleshooting, then disable it with `set web off`.\r\n- Browsers may still require accepting the self-signed certificate warning on first HTTPS access.\r\n- `flood.max.unscoped` defaults to `64`; lower it only if you want to limit how far unscoped flood traffic repeats.\r\n- `flood.max.advert` controls the hop limit for flooded advert packets.\r\n- Wi-Fi credentials are now mirrored into ESP32 NVS for OTA-safe recovery.\r\n\r\n### Documentation\r\n\r\n[xjarid.github.io/MeshCore-EastMesh](https://xjarid.github.io/MeshCore-EastMesh/)\r\n\r\n### Flashing\r\n\r\n- [flasher.eastmesh.au](https://flasher.eastmesh.au/)\r\n- [flasher.meshcore.io](https://flasher.meshcore.io/)\r\n\r\n### Assets\r\n\r\n- `.bin` - standard update image\r\n- `-merged.bin` - full ESP32 image for flashing after erase"
