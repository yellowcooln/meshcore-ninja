source: github
repo: Yeraze/meshmonitor
updatedAt: '2026-08-15T20:31:00.888Z'
releases:
  - version: v4.14.2-rc3
    name: v4.14.2-rc3
    datetime: '2026-08-15T19:42:23Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.2-rc3
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.2-rc3


      > ⚠️ **Pre-release.** This is a release candidate for testing. It is not tagged `:latest` and
      is not recommended for unattended production use.


      Nine changes since rc2, dominated by a mobile-freeze fix and two new terrain/RF surfaces.
      Opening a node with a large estimated-position history could lock the UI for about a minute on
      mobile — that is fixed, and the fix bounds both halves of the cause rather than the one that
      was reported. Predictive RF coverage arrives as a Fresnel + path-loss model with a radial
      terrain sweep and a map layer, deliberately drawn to look unlike measured coverage so a
      prediction is never mistaken for a measurement; note its control surface is not wired up yet,
      so the layer is present but not reachable from the UI in this build. A passive network survey
      now reports what a radio can actually hear — direct neighbours, signal, and reach by hop count
      — generating no radio traffic to do it. MeshBeacon offers (firmware 2.8+) become actionable
      invitation cards, with an explicit confirmation before any channel is written to a radio. Two
      protocol-level bugs are fixed: MeshCore's implicit Public channel was being displaced when
      adding a channel, and MeshBeacon module config failed to load with "Unknown config type".
      Traditional Chinese joins the language selector — it was already the most complete translation
      in the repository and simply had never been offered.


      ## Features


      - **Predictive RF coverage** — Fresnel + path-loss model, radial terrain sweep, API and map
      layer (#4741, refs #4727)

      - **Passive network survey** — reach, direct neighbours and hop distribution (#4737, closes
      #4726)

      - **Actionable MeshBeacon invitation card** — add / dismiss, with confirmation before any
      device write (#4735, closes #4723)

      - **Waypoint-anchored geofences** for enter/exit alerts (#4732, closes #4722)

      - **Incomplete-NodeInfo badge** on nodes missing name or hardware info (#4730, closes #4720)

      - **Traditional Chinese** in the language selector (#4742)


      ## Bug Fixes


      - **Map froze for ~1 minute** when opening a node with a large estimated-position history
      (#4744, closes #4743)

      - **MeshBeacon config load failed** with "Unknown config type: meshbeacon" (#4740, closes
      #4739)

      - **MeshCore Public channel displaced** when adding a new channel (#4734, closes #4733)

      - **Automation Runs log** now shows the captured triggering message (#4716, closes #4711)


      ## Issues Resolved


      #4743, #4739, #4733, #4726, #4723, #4722, #4720, #4711


      ## New Contributors


      - @rncchen made their first contribution in #4742 — Traditional Chinese language support


      ## Notes


      - **Predictive RF coverage has no UI entry point in this build.** The model, sweep, API and
      map layer are complete and tested; the site-planner control panel that drives them is still to
      come, so #4727 stays open.

      - Coverage predictions are modelled from terrain only — they account for free-space loss,
      earth curvature and diffraction, but not buildings, foliage or multipath. Treat them as
      planning aids, not measurements.

      - Very dense position histories are now drawn simplified; the trail's reported time span
      remains exact.


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.14.2-rc2...v4.14.2-rc3


      ## 🚀 MeshMonitor v4.14.2-rc3


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.2-rc3
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.2-rc2
    name: v4.14.2-rc2
    datetime: '2026-08-14T19:33:20Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.2-rc2
    prerelease: true
    notes: >-
      ## What's Changed

      * feat(reticulum): Phase 1b — frontend + deployment (#3960) by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4698

      * fix(automation): let MT↔MC bridge relay the operator's own messages by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4695

      * fix(unified): gate telemetry on nodes:read, fetch MeshCore nodes by publicKey by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4701

      * fix(docker): skip puppeteer's Chrome download in the main image by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4707

      * chore(deps-dev): bump @types/better-sqlite3 from 7.6.13 to 9.6.0 by @dependabot[bot] in
      https://github.com/Yeraze/meshmonitor/pull/4652

      * chore(deps): bump lucide-react from 1.29.0 to 1.30.0 by @dependabot[bot] in
      https://github.com/Yeraze/meshmonitor/pull/4653

      * feat(reticulum): Phase 2 — LXMF messaging (#3960) by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4706

      * fix(automation): mark tapback/reply text on the MT→MC bridge by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4699

      * Translations update from Hosted Weblate by @weblate in
      https://github.com/Yeraze/meshmonitor/pull/4688

      * docs(add-ons): list CardMesh and document the REST API integration path by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4708

      * feat(reticulum): Phase 3 — position, telemetry & own-mode radio (#3960) by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4713

      * feat(admin): warn when position precision exceeds the public-channel clamp (#4705) by
      @Yeraze in https://github.com/Yeraze/meshmonitor/pull/4710

      * fix(tcp): propagate asynchronous socket write failures by @gdiciocco in
      https://github.com/Yeraze/meshmonitor/pull/4692

      * fix(pkg): declare the supported Node range in engines (#4703) by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4714

      * Fix traceroute channel resolution: 3 bypassing paths + PSK/name gaps by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4712

      * feat(reticulum): Phase 4 — topology & remote monitoring (#3960) by @Yeraze in
      https://github.com/Yeraze/meshmonitor/pull/4715

      * docs(reticulum): close out epic — Phase 4 done, Phase 5 (desktop parity) descoped (#3960) by
      @Yeraze in https://github.com/Yeraze/meshmonitor/pull/4717

      * chore(release): 4.14.2-rc2 by @Yeraze in https://github.com/Yeraze/meshmonitor/pull/4719


      ## New Contributors

      * @gdiciocco made their first contribution in https://github.com/Yeraze/meshmonitor/pull/4692


      **Full Changelog**: https://github.com/Yeraze/meshmonitor/compare/v4.14.2-rc1...v4.14.2-rc2

      ## 🚀 MeshMonitor v4.14.2-rc2


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.2-rc2
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.2-rc1
    name: v4.14.2-rc1
    datetime: '2026-08-13T18:33:48Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.2-rc1
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.2-rc1


      > ⚠️ **Pre-release**, release candidate for testing. Do not use in production.


      First release candidate of the 4.14.2 cycle. Two feature efforts landed: the

      groundwork for **Reticulum** as a third protocol source, and the completion of

      **Meshtastic 2.8** support, which closed the last three 2.8 tracking issues.


      ### New features


      - **Reticulum (RNS) source, Phase 1a: bridge + backend** (#4690, part of #3960), first phase
      of adding Reticulum as a third first-class protocol source alongside Meshtastic and MeshCore.
      Ships a `meshmonitor-rns-bridge` Python sidecar (its own `python:3.12-alpine` image; the main
      `node:24` image is untouched) speaking versioned JSON over a loopback WebSocket to a Node
      `ReticulumManager` implementing `ISourceManager`. Two connection modes, `attach` (client of a
      running `rnsd`) and `tcp_peer`. Migrations 141/142 add `reticulum_destinations` and
      `reticulum_interfaces`. Interface throughput history rides the shared `telemetry` table, so
      Dashboard reuse comes for free. A Reticulum source is creatable via the API and fully
      functional at the backend/bridge layer, **the source-creation UI lands in Phase 1b**, so there
      is no frontend for it in this RC.


      - **MeshBeacon is now configurable, not just decodable** (#4689, closes #3854), firmware 2.8's
      `MESH_BEACON_APP` broadcast already decoded into the Packet Monitor; this adds a
      `MeshBeaconConfig` editor on both surfaces that edit module configs (the Configuration page
      and the Admin Commands tab). Listen/broadcast/legacy-split toggles, the beacon text, and what
      network to advertise. Gated at firmware 2.8.0, so older devices show the section greyed out
      rather than accepting a save the device silently drops.


      - **MeshBeacon automation trigger** (#4689), a `trigger.meshBeacon` block, filterable by
      beacon text or narrowed to beacons that actually advertise a network. Beacons are never stored
      as messages, so they were previously invisible to the Automation Engine.


      - **XEdDSA signing status on messages** (#4689, closes #3923), the signed-broadcast shield now
      appears on messages, alongside the existing MQTT and store-and-forward indicators, completing
      the packet-level shield that shipped in 4.14.1. Migration 140 persists the flag so it survives
      a reload. MeshMonitor does not verify the signature itself; it surfaces what the node already
      checked.


      ### Under the hood


      - **Firmware 2.8 NodeDB warm tier reviewed** (closes #3548), no code changes needed, verified
      rather than assumed: MeshMonitor never parses `deviceonly.proto`, so the new `snr_q4` on-disk
      encoding cannot affect it; a refused `set_favorite` already surfaces through the admin ACK
      path without retrying; and the node-count drop on nRF52 plus blocked-node persistence were
      already documented.


      - Translations update from Hosted Weblate (#4684).


      ### Issues resolved


      - #3854 Track: MeshBeacon support (MESH_BEACON_APP, Meshtastic 2.8)

      - #3923 Feature: display packet signing status (XEdDSA, firmware 2.8+)

      - #3548 [2.8 tracking] NodeDB warm tier, node count changes, snr_q4 protobuf field, blocked
      node persistence


      ### Upgrade notes


      Three new migrations (140, 141, 142) run automatically on first boot across

      SQLite, PostgreSQL, and MySQL. All three are additive, a new nullable column on

      `messages` and two new Reticulum tables, so no existing data is rewritten.


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.14.1...v4.14.2-rc1


      ## 🚀 MeshMonitor v4.14.2-rc1


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.2-rc1
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.1
    name: v4.14.1
    datetime: '2026-08-13T12:27:59Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1
    prerelease: false
    notes: >-
      # MeshMonitor v4.14.1


      The headline of this release is the **Meshtastic ↔ MeshCore Automation Bridge**, you can now
      relay text between a Meshtastic channel and a MeshCore channel with a single click, built
      entirely on the Automation Engine rather than a native bridge. A new in-app **Automation
      Template Gallery** ships click-to-install templates (an **MT↔MC Bridge** and an **Auto-Ack**),
      a new `{{ trigger.protocolShort }}` token tags relayed messages `MT@`/`MC@`, and layered
      loop/flood safeguards (a per-automation rate limit, a MeshCore cross-source self-guard, and a
      content guard) protect against the amplifying-loop failure mode. The release also completes a
      large **theme-system overhaul**: colors are now named by *role* rather than by swatch, with
      new categorical and sequential color scales and a full migration off hardcoded palette values.
      MeshCore gains a **strict receive-only mode**, Analyzer Observer battery/uptime/noise
      reporting, static MQTT auth, and several delivery/pathing fixes. Messaging picks up an unread
      divider with jump-to-first-unread and channel-list unread badges. Rounding it out: position
      "last updated" display and estimate-anchor rationale, a fistful of map and MQTT fixes, a
      Windows startup fix, ESM/build hardening, and the usual dependency and translation updates.


      ## 🌉 Meshtastic ↔ MeshCore Automation Bridge (#4577)

      - In-app **Automation Template Gallery**, click-to-install templates that land disabled for
      review (#4679)

      - **MT↔MC Bridge** template, scaffolds a matched automation pair with a built-in loop guard
      (#4681); wizard pickers scoped per-protocol (#4683)

      - **Auto-Ack** template, acknowledge direct/zero-hop messages with a tapback or reply (#4679)

      - `{{ trigger.protocolShort }}` token → `MT` / `MC` (#4675)

      - Loop & flood safety: per-automation rate limit + MeshCore cross-source self-guard + content
      guard (#4676)

      - User docs & recipe with flood-safety guidance (#4682)


      ## ✨ Features

      - Automation Engine: **became-mobile** & **left-home** triggers for tamper/theft monitoring
      (#4648, thanks @gztproject)

      - Automation Engine: distinct reaction emoji for MQTT-sourced pings (#4601)

      - MeshCore: **strict receive-only mode**, backend enforcement, frontend UX, and Virtual Node
      gating (#4550, #4552, #4555)

      - MeshCore: Analyzer Observer publishes battery / uptime / noise floor (#4557); static MQTT
      username/password auth (#4602)

      - Messages: unread divider + jump-to-first-unread for both protocols (#4615); channel-list
      unread bell badge (#4669)

      - Packet inspection detail view on message hop-count click (#4664)

      - Show when a node's position was last updated (#4663); record & expose the anchors behind an
      estimated position (#4613)

      - Security: set a node's private key from Device Configuration (#4654)

      - Themes: categorical color scale (#4605) and sequential scale (#4611)

      - Ko-fi support links in the sidebar and site nav (#4666, #4665); User Scripts Gallery, Border
      Wait Times (#4686)


      ## 🐛 Bug Fixes

      - MeshCore: deliver MeshMonitor-originated channel messages to app clients (#4668); preserve
      newlines in message bodies (#4661); update contact Last Heard on incoming DM (#4554); treat a
      lost out_path ack as success (#4628); hot-swap Analyzer Observer publisher on signing-key
      change (#4544)

      - MQTT: fire message events & alerts for MQTT-sourced messages so the Automation Engine and
      alerts trigger (#4600)

      - Map: single card shell for node popups (#4680); reduce statistical-route edge overlap
      (#4678); gate marker clicks on density, not zoom alone (#4563); stop shading
      pending-traceroute nodes as 0-hop/local (#4576); fit-bounds icon fix (#4564)

      - Nodes: stop dropping distinct nodes with a non-numeric nodeNum in the unified view (#4578);
      clear auto-favorite, auto-traceroute, and auto-time-sync allowlists on node purge (#4651,
      #4630)

      - Auto-ack: stop acknowledging tapbacks (#4574); key the tapback guard on the emoji flag, not
      its presence (#4589)

      - Neighbor Info: surface why a request was rejected (403/429) (#4575)

      - Position

      …
  - version: v4.14.1-rc5
    name: v4.14.1-rc5
    datetime: '2026-08-12T15:30:02Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1-rc5
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.1-rc5


      > ⚠️ **Pre-release** — release candidate for testing. Do not use in production.


      Small focused bump over rc4 to get the proxy-auth session-fixation fix out to Windows desktop
      users, plus one MeshCore Virtual Node delivery fix.


      ### Security fixes


      - **Regenerate session on proxy-auth identity binding** (#4672) — the proxy-auth path in
      `authMiddleware` used to write user identity into whatever session came in with the request,
      without rotating the session ID. On deployments with `PROXY_AUTH_ENABLED=true` (Authelia,
      oauth2-proxy, Cloudflare Access, Authentik, etc.) this let an attacker who could plant a
      `meshmonitor.sid` cookie on a victim's browser ride the post-authentication session. Now
      regenerates the session ID on the first identity bind, mirroring what local/MFA/OIDC login
      already did. CSRF token is preserved across regeneration. If `PROXY_AUTH_ENABLED=false` (the
      default), no impact.


      ### Bug fixes


      - **MeshCore-VN: deliver MeshMonitor-originated channel messages to VN clients** (#4668,
      external contribution from @pfmos) — closes #4535. When a MeshCore Virtual Node was running,
      channel messages sent from MeshMonitor's web UI (or from another connected MeshCore app
      client) were silently dropped for every other connected client. Per-client attribution now
      delivers each message to every client *except* the one that originated it, so all connected
      apps see the same conversation. DMs stay suppressed with an in-docs explanation of the
      companion-protocol constraint.


      ### Issues resolved


      - #4535 [BUG] Messages sent to MeshCore Virtual Node app only work in one direction


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.14.1-rc4...v4.14.1-rc5


      ## 🚀 MeshMonitor v4.14.1-rc5


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.1-rc5
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.1-rc4
    name: v4.14.1-rc4
    datetime: '2026-08-12T03:35:00Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1-rc4
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.1-rc4


      > ⚠️ **Pre-release** — release candidate for testing. Do not use in production.


      Small quality-of-life pass over rc3: three new UI surfaces (packet detail on hop-count click,
      per-node position freshness, unread-count bell), a MeshCore chat bugfix, and a Ko-fi Support
      link in both the site nav and the app sidebar.


      ### Features


      - **Packet inspection detail on message hop-count click** (#4657, #4664) — click any message's
      hop-count badge to open the underlying packet's full metadata (headers, encryption context,
      route).

      - **"Position updated" timestamp in Node Details + map popup** (#4662, #4663) — a subtle
      "Updated 3h ago" line beneath the coordinates, distinct from the generic "Last Heard", so a
      stale fix on an otherwise-chatty node reads at a glance.

      - **Bell icon on channel-list unread badges** (#4660, #4669) — the unread count now renders
      with a `🔔` glyph so it's obvious at a glance which channel has activity.

      - **Ko-fi Support link in the app sidebar footer** (#4666) — heart icon next to the existing
      Discord/GitHub/Website links.


      ### Bug fixes


      - **MeshCore chat now preserves newlines** (#4659, #4661) — multi-line messages no longer
      collapse to a single line in the MeshCore stream.

      - **Sidebar footer keeps its icons on one line after the Ko-fi addition** (#4667) —
      narrow-rail regression fix.


      ### Docs & site


      - **Ko-fi donation link in the meshmonitor.org site nav** (#4665) — same heart icon next to
      Discord/GitHub in the top-right.


      ### Issues resolved


      - #4657 Feature: packet inspection detail view on message hop count click

      - #4659 [BUG] Meshcore new lines not being rendered

      - #4660 [FEAT] Add unread message badges/counts to channel list

      - #4662 Show when a node's position was last updated in Node Details


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.14.1-rc3...v4.14.1-rc4


      ## 🚀 MeshMonitor v4.14.1-rc4


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.1-rc4
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.1-rc3
    name: MeshMonitor v4.14.1-rc3 — Movement Triggers
    datetime: '2026-08-10T16:57:03Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1-rc3
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.1-rc3 — Movement Triggers


      > ⚠️ **Pre-release.** This is a release candidate for 4.14.1. It ships on the `:dev` Docker
      tag, not `:latest`. Run it if you want the changes early and can tolerate rough edges; report
      anything you hit.


      ## Summary


      This candidate adds two Automation Engine triggers built for **stationary GPS fleets** —
      rooftops, towers, and gateways you want to hear about the moment they move. Pair either with a
      Send-message or Notify action for a tamper / theft alert.


      - **Left home** is the primary theft detector. It fires when a watched node drifts farther
      than a threshold (default **300 m**) from a home anchor MeshMonitor learns per node and stores
      in the database — so a restart never silently re-homes a node that was carried off. The anchor
      is seeded from position-history inliers (dropping GPS spider-lines), gently self-corrects
      while the node sits still, and can be reset from history at any time.

      - **Became mobile** is a coarser companion: it fires when a hand-selected node's mobility flag
      flips from stationary to moving.


      Also in this candidate: you can now **set a node's private key from Device Configuration** —
      for restoring an identity from backup or moving it to replacement hardware — and node purges
      now clean up the automation bookkeeping they used to leave behind.


      ## Features


      - **Left-home / tamper trigger** — per-(automation, node) home anchors persisted across
      restarts, history-inlier seeding, EMA self-correction near home, a **Reset homes from
      history** action, a stationary-first node picker, and `{{ node.longName }}` / `{{ node.nodeId
      }}` message tokens. (#4648)

      - **Became-mobile trigger** — fires on a stationary → mobile flip (MeshMonitor's >100 m
      position-history heuristic) for hand-selected nodes. (#4648)

      - **Set a node's private key from Device Configuration** — local device only, X25519; on a
      change MeshMonitor derives the matching public key and writes both, so the node can never be
      left advertising a key that no longer matches its secret. Guarded behind a confirmation that
      spells out the impersonation / duplicate-key risk. (#4632, #4654)


      ## Fixes


      - **Node purge now clears its automation bookkeeping** — auto-favorite targets and assignments
      (#4633), and auto-traceroute / auto-time-sync allowlists (#4630), so a re-added node no longer
      inherits stale state.

      - **MeshCore:** a lost `out_path` acknowledgement is treated as success, not failure. (#4625)


      ## Docs


      The [CHANGELOG](https://github.com/Yeraze/meshmonitor/blob/main/CHANGELOG.md) now covers the
      full 4.14.1 rc1 → rc3 history, and the feature guides were reconciled with what shipped across
      the candidates (Analyzer Observer static MQTT auth, the new Device Configuration security
      section, position-estimation anchors, and the theme chart color scales).


      ## Upgrade


      Docker `:dev` users receive this automatically.


      ## 🚀 MeshMonitor v4.14.1-rc3


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.14.1-rc3
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.14.1-rc2
    name: Theme Color Schemas
    datetime: '2026-08-09T18:41:08Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1-rc2
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.1-rc2 — Theme Color Schemas


      > ⚠️ **Pre-release.** This is a release candidate for 4.14.1. It ships on the `:dev` Docker
      tag, not `:latest`. Run it if you want the changes early and can tolerate rough edges; report
      anything you hit.


      ## Summary


      This candidate is one piece of work: **custom themes are now defined by what a colour is
      _for_, not which Catppuccin swatch it happens to be.**


      Before, a theme was 26 palette names — `base`, `blue`, `mauve`, `peach`. To make the accent
      red you had to write `blue: "#ff0000"`, and anyone reading that theme afterwards had to know
      `blue` meant "the accent". A theme is now 26 roles:


      ```jsonc

      { "accent": "#ff0000", "bg": "#1e1e2e", "textFaint": "#6c7086", "error": "#f38ba8" }

      ```


      **Existing custom themes keep working.** Definitions stored in the old format are translated
      when they're read — nothing is rewritten on disk, so downgrading is safe.


      Getting there also fixed a family of bugs where colours were pinned to Catppuccin Mocha
      regardless of the theme you'd selected. Tinted panel backgrounds, warning banners, badge fills
      and drop shadows all rendered Mocha's palette on Nord, Gruvbox, Solarized and every other
      theme. If you use a non-Catppuccin theme, this is the release where those surfaces finally
      match it.


      Two smaller wins fell out of the same work. Roles that used to share one swatch — faint text,
      subtle borders and the inactive-element fill were all `overlay0` — can now be themed apart, so
      a theme can lighten its chrome without also washing out timestamps. And chart colours are
      drawn from a dedicated categorical scale chosen to stay mutually distinguishable in every
      built-in theme, instead of borrowing role colours that could collide.


      ## Features


      - **Themes are authored by role** (`accent`, `bg`, `textFaint`, `error`) instead of by
      Catppuccin swatch name, with the theme editor grouped and labelled to match (#4623). Closes
      #4567.

      - **Categorical colour scale** for charts and per-source tagging, kept distinct from the role
      vocabulary so a theme's accent colour can never collide with a data series (#4605, #4606).

      - **Sequential colour scale** for magnitude — distance histograms and hop-density ramps now
      read as ordered rather than as a set of unrelated hues (#4611).


      ## Bug Fixes


      - **Tinted backgrounds ignored the active theme** — the variables behind them were never
      defined, so every tint silently fell back to hardcoded Catppuccin Mocha in all 15 themes
      (#4617).

      - **87 hardcoded palette colours** written directly as `rgba()` values now follow the theme.
      The worst of these put a Mocha-green background behind theme-coloured text on the same badge,
      so the two halves disagreed on any other theme (#4620).

      - **Chart backgrounds, gridlines and labels** read theme colours correctly (#4622).

      - **Remaining hardcoded colours in components** — hover shades that shifted hue instead of
      brightness, and status colours that had no theme-aware definition (#4619).

      - **Estimated-position anchors** are written atomically, with the empty-input contract
      documented (#4621). Closes #4614.


      ## Documentation


      - Theme gallery regenerated against the new stylesheet, so the screenshots come from code that
      still exists (#4624).


      ## Upgrade notes


      - **Custom themes**: no action needed. Old swatch-keyed definitions are translated on read. If
      you re-save a theme in the editor it will be written in the new role format; before that, the
      stored row is untouched.

      - **Built-in themes are visually unchanged.** This was verified rather than eyeballed — both
      stylesheets were resolved through a real browser across 15 themes × 39 colour tokens, giving
      585 comparisons and zero differences. The deliberate exceptions are two rules (timestamps and
      small field labels) that moved to the faint-text role, which had no correct home before.


      ## 🚀 MeshMonitor v4.14.1-rc2


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        
      …
  - version: v4.14.1-rc1
    name: v4.14.1-rc1
    datetime: '2026-08-05T18:02:54Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.1-rc1
    prerelease: true
    notes: >-
      # MeshMonitor v4.14.1-rc1


      > ⚠️ **Pre-release.** This is a release candidate for 4.14.1. It ships on the `:dev` Docker
      tag, not `:latest`. Run it if you want the fixes early and can tolerate rough edges; report
      anything you hit.


      ## Summary


      This candidate is mostly MeshCore work plus two map-interaction fixes. The headline feature is
      **strict receive-only mode** for MeshCore Companion sources — a per-source switch that holds
      every transmitting command, scheduler and automation while leaving receive, the packet log,
      the Analyzer Observer and local serial configuration fully working. Because MeshCore firmware
      has no radio-level transmit switch, this is enforced in software; link-layer acknowledgements
      and any advert schedule configured outside MeshMonitor are unaffected, and the UI says so
      plainly. The Analyzer Observer gained battery, uptime and noise-floor reporting, and it now
      hot-swaps its publisher when you import a signing key instead of demanding a source
      disable/re-enable cycle. On the permissions side, **MeshCore contact positions now honor
      `canViewOnMap`**, matching how Meshtastic nodes have always behaved — a migration backfills
      the flag so no existing user loses map access on upgrade. Two map fixes: clicking a marker now
      zooms in first only when the marker is genuinely crowded (isolated markers open their popup
      immediately at any zoom), and the threshold that governs it is configurable rather than
      hardcoded at z13. The zoom-to-fit button also stopped wearing a crosshair icon, which everyone
      reasonably read as "center on my GPS location."


      ## Features


      - **MeshCore strict receive-only mode** — per-source enforcement (#4550), UI (#4552), and
      Virtual Node gating plus docs (#4555). Closes #4547.

      - **Analyzer Observer publishes battery, uptime and noise floor** in its status payload
      (#4557). Closes #4556.


      ## Bug Fixes


      - **MeshCore map positions now require `canViewOnMap`** instead of only `nodes:read`, so a
      read-only user can see the contact list without learning where those nodes are (#4560). Closes
      #4559.

      - **Marker clicks gate on density, not zoom alone** — an isolated marker opens its popup on
      the first click at any zoom, and the crowding threshold is now a setting (#4563). Closes
      #4551.

      - **Zoom-to-fit no longer uses the locate-me crosshair icon** (#4564). Closes #4562.

      - **MeshCore contact Last Heard updates on incoming direct messages** (#4554). Closes #4553.

      - **Analyzer Observer hot-swaps its publisher on a signing-key change** — no more
      disable/re-enable dance after importing a key (#4544). Closes #4543.

      - **DeviceMetadata arriving before MyNodeInfo is buffered** rather than dropped (#4548).

      - **Remote admin stops implying it knows a remote node's favorite/ignored state** — the admin
      protocol has no readback for those flags, so the UI no longer pretends otherwise (#4542).
      Closes #4511.


      ## Documentation


      - Documented the new **Map Click Zoom Gate** setting and the MeshCore `canViewOnMap` change,
      including the upgrade backfill (#4565).

      - Site gallery: added **Norway Østlandet Mesh** (#4546). Closes #4545.


      ## Tests / CI


      - **System tests moved off the v1 API root paths removed in 4.14** (#4565). The
      `/api/v1/nodes`-style root paths were deleted in 4.14 (#4117); because a 404 body isn't JSON,
      every assertion was dying inside `jq` with an opaque parse error. The same change corrects
      `/api/upgrade/*` from the transitional `410` to its post-removal `404`. Nothing user-facing —
      but the suite is honest again.


      ## Translations


      - Weblate updates (#4541, #4252) — thank you to everyone translating.


      ## Upgrade Notes


      - **Migration 135** backfills `canViewOnMap` on existing `nodes` grants for MeshCore sources
      that already had read access, so current users keep map visibility across the upgrade. New
      grants must set the flag explicitly.

      - Receive-only mode is **per source** and defaults to off. Nothing changes unless you turn it
      on.


      ## Thanks


      - **@wilhel1812** for reporting the zoom-to-fit icon confusion in Discord (#45

      …
  - version: v4.14.0
    name: v4.14.0
    datetime: '2026-08-03T18:26:58Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.14.0
    prerelease: false
    notes: >-
      # MeshMonitor v4.14.0


      The first stable release since **v4.13.2** — the 4.13.3 release candidates were folded into
      this minor instead, because the work that landed is a minor's worth, not a patch's. The
      headline is the **MeshCore Analyzer Observer**: publish what your Companion hears to a
      regional analyzer without a second app fighting for the serial port, observation-only by
      design. Meshtastic and MeshCore sources now share **one navigation system**, which on phones
      means a bottom bar that scrolls sideways with readable labels instead of eleven crushed icons.
      Remote admin gained **distinct ACK outcomes** — rejected reads differently from timed out —
      plus opt-in auto-retry, and commands no longer die when the browser navigates away. MeshCore
      messages carry **SNR and RSSI** when heard directly, "Discover Nodes" now lists who answered
      with signal in both directions, and per-channel permissions finally do what they say. Node
      Details shows **where a position came from and how precise it is**, and Null Island estimates
      are no longer passed off as real fixes. The Packet Monitor's node filters became searchable
      comboboxes, which matters most on a phone and on a large mesh. Under the hood, the database
      driver moved to **better-sqlite3 13** (N-API), verified at runtime on amd64, arm64 and armv7
      before shipping.


      ## ⚠️ Breaking Change: v1 API root paths removed


      4.13 moved the v1 mesh-data endpoints under `/api/v1/sources/{sourceId}/…` and kept the old
      root paths alive for one release behind a `Warning: 299` header. **That grace period ends
      here** — these paths now return **404**:


      ```

      /api/v1/nodes      /api/v1/telemetry     /api/v1/network

      /api/v1/messages   /api/v1/traceroutes   /api/v1/status

      /api/v1/channels   /api/v1/packets       /api/v1/position-history

      ```


      Migrate by moving the source into the path:


      ```bash

      # Before (4.13, deprecated)

      curl -H "Authorization: Bearer mm_v1_…" "https://host/api/v1/nodes?sourceId=abc123"


      # After (4.14)

      curl -H "Authorization: Bearer mm_v1_…" "https://host/api/v1/sources/abc123/nodes"

      ```


      `GET /api/v1/sources` lists your source ids, or use the literal `default` to target the
      primary source. The `/api/upgrade/*` endpoints, returning `410 Gone` since 4.13, are also
      removed.


      **If you script against the REST API, check for these paths before upgrading.** Everything
      else is drop-in; the database schema migrates automatically on first boot.


      Full details in the [release
      announcement](https://meshmonitor.org/blog/2026-08-03-v4.14.0-release).


      ### Features


      - feat(analysis): show node names in the ok_to_mqtt violations report (#4343)

      - feat(i18n): localize the MapStyleManager settings component (#4363)

      - feat(meshcore): serve ExportPrivateKey over the Virtual Node behind an "Allow PKI export"
      gate (#4366)

      - feat(automations): hop-count tapback + {{ trigger.hopEmoji }} (#4340 phase 1) (#4390)

      - feat(traceroute): visual node strip on the Node Details panel (#4392)

      - feat(automations): per-node cooldown scope (#4340 phase 2) (#4396)

      - feat(meshcore): add per-node ignore list to the auto-responder (#4402)

      - feat(map): per-overlay toggle to disable the GeoJSON click popup (#4403)

      - feat(meshcore): add zero-hop ping action (#4404)

      - feat(tx-guard): allow sends when UDP Broadcast can relay for a TX-disabled node (#4405)

      - feat(waypoints): let the user pick the broadcast channel (#4406)

      - feat(automations): Auto-Ack parity conditions (#4340 phase 3) (#4407)

      - feat(automations): Auto-Acknowledge → Automation converter (#4340 phase 4) (#4408)

      - feat(settings): per-source settings foundation and guardrails (#4412 Phase 1) (#4417)

      - feat(traceroute): interactive strip — node popup More Details action + link tooltips (epic
      phase 1) (#4424)

      - feat(settings): read Node Display settings per-source (#4412 Phase 2) (#4425)

      - feat(traceroute): participation picker on all sources — brings the strip to MQTT (epic phase
      2) (#4427)

      - feat(traceroute): Copy Forward/Return/Both text links with insecure-context fallback (#4429)

      …
  - version: v4.13.3-rc5
    name: v4.13.3-rc5
    datetime: '2026-08-01T18:42:51Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.3-rc5
    prerelease: true
    notes: >-
      Release candidate 5 for 4.13.3. Twelve changes since rc4.


      ## Remote administration no longer blocks on the mesh


      Issue #4482 — a single remote admin action could legitimately hold a browser HTTP request open
      for up to **75 seconds** (up to 45s acquiring the session passkey, plus up to 30s awaiting a
      routing ACK). Behind a reverse proxy or CDN that surfaced as an upstream 502 with an unhelpful
      "Request failed".


      Remote admin commands, session-passkey acquisition, and remote config import now return `202
      Accepted` with an operation id and complete in the background, polled via `GET
      /api/admin/operations/:id`. Local-node commands stay synchronous — they have no passkey step
      and no ACK wait.


      - **Remote ignore/unignore now confirm.** They wait for the routing ACK exactly as favorites
      do, so a remote ignore can be verified rather than "succeeding" instantly with no way to
      check.

      - **Partial config imports report what failed**, not just what landed — "1 channel" is now
      distinguishable from "1 of 2 channels".


      (#4485, #4486; documented in #4490)


      ## Admin messages honor the configured hop limit


      Admin packets were built with a hardcoded `hopLimit: 3` — the firmware default, not the user's
      setting. Anyone who raised their node's LoRa hop limit to reach a deeper mesh still had every
      remote admin command expire 3 hops out, with no indication why. They now use the node's
      configured value, matching how the Meshtastic Python client behaves. (#4479)


      ## Fixes


      - `GET /stats` returned 500 whenever `sourceId` was omitted — the cross-source aggregate path
      had never worked (#4470)

      - MeshCore: anon auth banner, channel-sync data loss, reply/trigger ordering (#4491)

      - MeshCore: channel view now opens at the top, and Delete no longer appears to do nothing
      (#4488)

      - MeshCore: hop/route/scope info preserved in channel message history (#4475)

      - Older-message loads no longer force-scroll the view to the bottom (#4477)

      - Restored the send-message button icon (#4480)


      ## Navigation


      - Unified Meshtastic/MeshCore per-source navigation (#4481)

      - Meshtastic phone navigation moved to the shared bottom bar (#4484)


      ## Validation


      Full system-test suite passed on hardware — all 12 legs, including **Configuration Import**,
      which exercises the newly-async `/api/admin/import-config` path and the hop-limit change
      against a real device. This is the first build to put the async remote-admin work in front of
      hardware; it shipped on unit and route coverage alone.

      ## 🚀 MeshMonitor v4.13.3-rc5


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.3-rc5
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.13.3-rc4
    name: v4.13.3-rc4
    datetime: '2026-07-31T22:43:05Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.3-rc4
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.3-rc4


      > ⚠️ **Pre-release.** This is a release candidate for testing. It does not carry the `:latest`
      Docker tag. Run it in production only if you are ready to report bugs.


      ## Summary


      This candidate is built around two threads: the **MeshCore Analyzer Observer** epic (#4457)
      landing in full across three phases, and a chain of **position-trust** fixes that started with
      a protobuf refresh and ended up closing a security hole plus a long-standing data bug.


      The position thread is worth reading in order. Refreshing the Meshtastic protobufs picked up
      `MeshPacket.rx_rssi` gaining *explicit presence* — which turned a latent ambiguity into a live
      defect. Every `rx_rssi != 0` guard had been treating a genuine 0 dBm reception as "no data",
      and in the local-node spoof detector that meant a 2.8 node transmitting at point-blank range
      was classified as **our own outgoing message**. Fixed, with a regression test on that exact
      case.


      Next, estimated positions were made honest. A trilaterated estimate used to be
      indistinguishable from a device GPS fix once it reached the client, because only one of the
      three position sources set a flag. Node Details now carries a **GPS / Estimated / Override
      pill and a ± accuracy radius**. On a typical install that relabels several hundred nodes that
      were quietly presenting guesses as fixes.


      That pill immediately exposed a real bug it had been hiding. Migration 107 once cleared bogus
      `(0,0)` fixes from `nodes`, but never swept `estimated_positions` — so a node whose Null
      Island fix had been cleared fell through to a Null Island *estimate*, re-substituting the
      exact coordinates the migration removed. **Migration 134** purges those rows, the estimator no
      longer generates them, and the display gate now accepts a genuine equator or prime-meridian
      coordinate instead of reading a zero as "unpositioned".


      ## Features


      - MeshCore Analyzer Observer — backend foundation (#4457 phase 1) — #4464

      - MeshCore Analyzer Observer — publisher service (phase 2) — #4468

      - MeshCore Analyzer Observer — UI and docs (phase 3, final) — #4471

      - Position provenance in Node Details: GPS / Estimated / Override pill plus a ± accuracy
      radius (#4432) — #4467

      - `DeviceMetadata.has_xeddsa` surfaced in Admin Commands, distinguishing "cannot sign" from
      "did not sign" (#3923) — #4466


      ## Bug Fixes


      - **Local-node spoof detection failed open on a 0 dBm reception.** With firmware 2.8's
      explicit `rx_rssi` presence, a spoofed packet at point-blank range was classified as a genuine
      local transmission — #4466

      - Null Island estimates presented as real positions; migration 134 purges the stored rows, the
      estimator stops producing them, and a genuine equator / prime-meridian fix is no longer
      overwritten by an estimate — #4469

      - PWA notification clicks now deep-link to the source route (#4463) — #4465

      - MeshCore channel message history: infinite-scroll pagination — #4461

      - Traceroute node picker: relayed participation is MQTT-only — #4459

      - Per-node uptime graphs humanized (#3261); sidebar controls no longer overlap the shared
      footer (#4436) — #4458


      ## Protocol / Internals


      - Protobufs refreshed to `develop@6ceceae` (61 upstream commits). HardwareModel 141–143
      (`HELTEC_RC32` / `RC52` / `RCC6`), `PortNum.LORA_OTA_APP = 79`, `UA_868` marked deprecated —
      #4466

      - CI/release watcher scripts retry transient GitHub API errors — #4462


      Still no upstream 2.8 protobufs tag (newest remains `v2.7.26`), so the pin is a `develop`
      preview and the 2.8 tracking issues stay open.


      ## Upgrade Notes


      - **Migration 134** runs automatically on first boot and deletes Null Island rows from
      `estimated_positions`. Any affected node loses its bogus estimate; the scheduled recompute
      recreates a row if a legitimate solve still exists.

      - Nodes that were displaying an estimated position will now show an `ESTIMATED` pill. That is
      the estimate becoming visible, not a new estimate.


      ## 🚀 MeshMonitor v4.13.3-rc4


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      …
  - version: v4.13.3-rc3
    name: v4.13.3-rc3
    datetime: '2026-07-30T21:27:47Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.3-rc3
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.3-rc3


      > ⚠️ **Pre-release.** This is a release candidate for testing. It does not carry the `:latest`
      Docker tag. Run it in production only if you are ready to report bugs.


      ## Summary


      This candidate closes out two epics and sweeps up the defects they exposed. The traceroute
      epic gains a **statistical route view**: route segments now aggregate across many traceroutes,
      a union layout engine draws the merged graph, and the result renders in the Node Details
      strip. The strip also picks up interactive node popups, link tooltips, Copy
      Forward/Return/Both text links, and a participation picker that finally brings the strip to
      MQTT sources. The **per-source settings** epic (#4412) lands in three phases — backend
      foundation, per-source reads, then the frontend — and ends with MeshCore node-age filtering
      built on top of it.


      That second epic uncovered a cluster of permission bugs worth calling out. Two divergent
      source-scoped resource lists meant per-source scoping was silently ignored for `settings`,
      `dashboard`, `info`, `audit`, and `security` (#4416), and new-user default grants were written
      globally when four of six are per-source resources, leaving them inert (#4448). Both are
      fixed. On the data side, Meshtastic 2.8's periodic rebroadcast of cached packets was inflating
      node "Last Heard" (#4192), and nodes without a real fix were appearing on the map — position
      estimates built from a single anchor are now excluded by default (#4450). MeshCore gets guards
      against overlapping auto-pathfinding calls and a DM permission leak to anonymous users.


      ## Features


      - Statistical route aggregation + union layout engine (traceroute epic phase 1) — #4444

      - Statistical route view in the Node Details strip (phase 2) — #4451

      - Interactive strip: node popup More Details action + link tooltips — #4424

      - Participation picker on all sources, bringing the strip to MQTT — #4427

      - Copy Forward/Return/Both text links, with an insecure-context fallback — #4429

      - Per-source settings foundation and guardrails (#4412 phase 1) — #4417

      - Read Node Display settings per-source (phase 2) — #4425

      - Per-source Node Display frontend (phase 3) — #4431

      - MeshCore per-source node-age filtering and Node Display settings — #4433


      ## Bug Fixes


      - Guard `lastHeard` against replayed position/telemetry packets — #4446

      - Exclude single-anchor position estimates by default — #4455

      - Purging position history now also clears the global position estimate — #4452

      - Make `settings` a per-source permission resource — #4443

      - Close five defects the per-source epic uncovered (#4442, #4437, #4438, #4449, #4448) — #4453

      - Close the per-source settings defects: global current-value compare, full-table scan — #4441

      - Scope MeshCore `/snapshot` messages field to `messages:read` — #4423

      - Guard MeshCore `startAutoPathfinding()` against overlapping calls — #4435

      - Disable Convert-to-Automation button for MeshCore sources — #4421

      - Route strip edges and labels around unrelated node glyphs — #4430

      - Bound `geofenceState` and `autoAckCooldowns` growth — #4413

      - Guard MQTT `handleUplink` against a client nulled mid-flight — #4414

      - Virtual node: fall back to persisted modem preset for the slot-0 channel name — #4415

      - Virtual node: stop duplicate MQTT client-proxy publishing — #4418

      - Humanize the Dashboard uptime widget and align DM chart padding — #4439

      - Unify the sidebar footer across dashboard and per-source views — #4440

      - Pass `showList` from every back-to-dashboard button — #4454


      ## Chores


      - Bump version to 4.13.3-rc3 — #4456


      ## Issues Resolved


      #3261, #4037, #4192, #4399, #4411, #4412, #4416, #4419, #4420, #4422, #4426, #4428, #4434,
      #4436, #4437, #4438, #4442, #4445, #4447, #4448, #4449, #4450


      ## Upgrade Notes


      No schema migrations and no breaking API changes in this candidate.


      One behavior change worth watching: **map pins may thin out after upgrading.** Position
      estimates derived from a single anchor are now excluded by default, so nodes that previously
      showe

      …
  - version: v4.13.3-rc2
    name: v4.13.3-rc2
    datetime: '2026-07-29T04:35:14Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.3-rc2
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.3-rc2


      > ⚠️ **Pre-release.** This is release candidate 2 for 4.13.3. It is published for testing and
      does **not** update the `:latest` Docker tag. Use `:4.13.3-rc2` explicitly if you want to try
      it.


      The headline is that **Auto-Acknowledge now lives in the Automation Engine**. A four-phase
      epic gave the engine everything Auto-Ack could do — hop-count reactions, per-node cooldowns,
      node-completeness and ignore-list conditions — and then added a converter that turns an
      existing Auto-Ack config into a real, editable automation. That means per-channel
      acknowledgement bodies, the thing #4340 asked for, are now just two rules rather than a second
      configuration axis bolted onto Auto-Ack.


      Alongside it: waypoints can pick their broadcast channel, MeshCore gains a zero-hop ping
      action and a per-node ignore list for the auto-responder, TX-disabled nodes can still send
      when UDP Broadcast can relay for them, and the Node Details panel gets a visual traceroute
      strip. No migrations, no configuration changes, no breaking changes.


      ## Auto-Acknowledge → Automation Engine (#4340)


      Four phases, each independently shippable:


      - #4390 — **Hop-count tapback.** `Send a tapback (reaction)` gained an *Emoji source* option:
      a fixed emoji, or the message's hop count (`*️⃣` direct, `1️⃣`–`7️⃣` above), using the same
      table Auto-Ack uses. Also adds the `{{ trigger.hopEmoji }}` token and a general `showIf`
      mechanism for conditional builder fields.

      - #4396 — **Per-node cooldown scope.** Triggers gained *Cooldown applies to*: the whole
      automation (unchanged default), each node separately, or each node per source. Previously a
      cooldown on a busy channel suppressed acks to every other sender.

      - #4407 — **Auto-Ack parity conditions.** A `node.completeness` tri-state field, `is one of` /
      `isn't one of` string operators, `isDM`/`viaMqtt` in the condition picker, and an optional DM
      resend cap. A machine-enforced parity table now proves the engine can express every Auto-Ack
      setting.

      - #4408 — **The converter.** A *Convert to an Automation…* button previews exactly what will
      be created, shows an honest report of what did and did not convert, and writes only on
      confirmation. Turning off Auto-Ack afterwards is opt-in (checked by default) and touches only
      the on/off switch — every other setting is preserved, so you can switch back.


      **Upgrade note:** nothing changes unless you use the converter. Existing Auto-Acknowledge
      configs keep working exactly as before, and existing automations are untouched — every new
      option defaults to the previous behaviour.


      ## Features


      - #4341 — Waypoints: pick which channel the waypoint broadcasts on

      - #4393 — MeshCore: zero-hop ping action

      - #4391 — MeshCore: per-node ignore list for the auto-responder

      - #4381 — Traceroute: visual node strip on the Node Details panel

      - #4344 — Map: per-overlay toggle to disable the GeoJSON click popup

      - TX guard: allow sends when UDP Broadcast can relay for a TX-disabled node


      ## Bug Fixes


      - MeshCore: sign-extend `traceContactPath` per-hop SNR bytes (negative SNR was being read as a
      large positive value)

      - #4389 — Map legend gets a usable mobile presentation

      - #4395 — Every dashboard sidebar link gets an icon

      - Traceroute: portal the strip hover popup and show the Map node card


      ## Known follow-ups filed during this cycle


      - #4399 — `geofenceState` and `autoAckCooldowns` are unbounded maps

      - #4400 — an `ok_to_mqtt` fail-closed test that flakes *open* under different test ordering

      ## 🚀 MeshMonitor v4.13.3-rc2


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.3-rc2
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.13.3-rc1
    name: v4.13.3-rc1
    datetime: '2026-07-28T14:52:22Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.3-rc1
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.3-rc1


      > ⚠️ **Pre-release.** This is release candidate 1 for 4.13.3. It is published for testing and
      does **not** update the `:latest` Docker tag. Use `:4.13.3-rc1` explicitly if you want to try
      it.


      A maintenance candidate focused on the map surfaces. The Map Analysis 3D view now has the
      view-state, follow, and layer-switching plumbing that the 2D view already had, and its
      basemaps actually load — the CSP was blocking several built-in tileset hosts. The tile
      selector gets a real mobile bottom sheet instead of a popover that mobile users could never
      reach, and the "Show ATAK Contacts" toggle finally survives a reload after being silently
      dropped by `/map-preferences`. On the Nodes list, Node Details is now a distinct labeled
      button on every row rather than an icon-click affordance people had to discover. Rounding it
      out: news-feed links to meshmonitor.org are absolutized, stable releases publish to the `:dev`
      tag alongside `:latest`, the unit suite runs roughly 2.5× faster, and a flaky packet-log test
      is fixed. No migrations, no configuration changes, no breaking changes.


      ## Bug Fixes


      - #4373 — Map Analysis 3D view gets the view-state, follow, and layer plumbing 2D has; layer
      gating keyed on the effective view mode; CSP `connect-src` widened so every built-in tileset
      host loads 3D basemaps (#4371)

      - #4383 — Tile selector renders as a mobile bottom sheet (#4380)

      - #4384 — "Show ATAK Contacts" map toggle persists (#4378)

      - #4385 — Every node row gets a distinct, labeled Node Details button (#4379)

      - #4377 — News-feed links to meshmonitor.org are absolutized

      - `activeStyleJson` wired into the MapAnalysis and MeshCore `BaseMap` consumers


      ## CI / DevOps


      - #4372 — Unit suite parallelized (~17.5 min → ~6.6 min) and the duplicate CI workflow retired

      - #4382 — Stable releases publish to the `:dev` tag as well as `:latest`

      - #4386 — De-flaked the packet-log since-timestamp filter test

      - #4388 — Version bump to 4.13.3-rc1


      ## Issues Resolved


      - #4371 — Map Analysis 3D view missing view-state, follow, and layer-switching plumbing that
      2D has

      - #4378 — "Show ATAK Contacts" map toggle never persists

      - #4379 — Replace icon-click affordance for Node Details with a distinct labeled button

      - #4380 — Tile selector popover hidden on mobile with no fallback UI


      ## Upgrade Notes


      None. No database migrations and no configuration changes — upgrade in place from 4.13.2.


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.13.2...v4.13.3-rc1


      ## 🚀 MeshMonitor v4.13.3-rc1


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.3-rc1
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.13.2
    name: v4.13.2
    datetime: '2026-07-27T21:21:31Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.2
    prerelease: false
    notes: >-
      # MeshMonitor v4.13.2


      This release finishes the **ATAK / CoT** work, teaches MeshMonitor to respect a node that
      cannot transmit, adds a **3D terrain view**, and catches gateways that uplink traffic their
      senders asked to keep off MQTT.


      **If you run PostgreSQL or MySQL, take this release.** Those two backends re-ran every
      migration on each boot, and migration 030 rebuilds `route_segments` from scratch — 865k rows
      deleted and reinserted on every restart for one reporting install. SQLite was never affected.


      See the [release blog post](https://meshmonitor.org/blog/2026-07-27-v4.13.2-release) for the
      narrative walkthrough, and the
      [CHANGELOG](https://github.com/Yeraze/meshmonitor/blob/main/CHANGELOG.md) for all 49 entries.


      ## Fix first: PostgreSQL / MySQL migration ledger


      - The `settingsKey` idempotency guard was SQLite-only, so PostgreSQL and MySQL replayed all
      128 migrations at every start. Both backends now record applied migrations in a ledger.
      (#4233)


      ## ATAK / CoT


      - **Packet decoding** for ATAK plugin portnum 72 (`TAKPacket`) and portnum 78 (`TAKPacketV2`,
      zstd-compressed against the vendored TAKPacket-SDK dictionaries) — position, GeoChat, markers,
      routes, CASEVAC, emergency. GeoChat messages persist into Messages. (#3691 Phase 1, #4317)

      - **ATAK contacts on every map** — team-colored callsign markers with a `Show ATAK Contacts`
      toggle, STALE badge after 15 minutes, purge after 24 hours. (#3691 Phase 2)

      - **CoT TCP feed** for ATAK / WinTAK — settings-gated, streams Cursor-on-Target XML covering
      ATAK contacts plus every positioned Meshtastic and MeshCore node from every source. No
      authentication or encryption; trusted-network only. (#3691 Phase 3)


      ## Receive-only nodes


      - MeshMonitor now honors `lora.tx_enabled`. A central server-side guard rejects every transmit
      path, the UI disables transmit-dependent controls with an explanation instead of failing at
      the radio, and config import reads live remote LoRa config so it cannot flip TX back on by
      accident. New [Receive-only mode](https://meshmonitor.org/features/receive-only-mode) guide.
      (#4294, #4315)


      ## MQTT


      - **ok_to_mqtt violation detection** — flags gateways that uplink packets from nodes that
      opted out, with 90 days of history and an **ok_to_mqtt Violations** report (per-gateway and
      per-packet views, node names, CSV export, SQL-side paging). (#4114, #4330, #4343)

      - **Configurable downlink hop-limit override** (0–7) on the embedded broker, replacing the
      zero-hop toggle. (#4081)

      - **Auto-delete-by-distance applies at ingest**, so an out-of-radius node never reaches the
      map. (#3900)


      ## Maps


      - **3D terrain view** on Map Analysis (MapLibre + server-side DEM proxy), with node markers,
      neighbor links, and traceroute paths in 3D and a clean fallback without WebGL. (#3826)

      - **Terrain Link Profile refinements** — antenna AGL seeded from node altitude, clickable thin
      neighbor links, terrain analysis on route segments. (#4253, #4254)

      - `ROUTER_LATE` is distinguishable from `ROUTER`, unmessageable nodes show a badge, and the
      Nodes tab is now called **Map**. (#4295, #4325)


      ## Also in this release


      - **Share a node as a Meshtastic contact** — QR code plus a `meshtastic.org/v/#…` contact
      link. (#4327)

      - **NodeInfo Enrichment report** — finds fields one source could fill from another and applies
      the fix; Copy NodeInfo gained per-field selection and overwrite. (#3837, #4244)

      - **MeshCore Virtual Node "Allow PKI export"** — a separate, default-off gate serving
      `ExportPrivateKey(23)` for tools that authenticate as the node itself. The VN port has no
      client authentication and the private key lets a client permanently become your node, so this
      is deliberately *not* folded into admin commands. See [Virtual Node
      docs](https://meshmonitor.org/configuration/virtual-node#pki-private-key-export-meshcore).
      (#3933, #4366)

      - **Every PKI DM was favoriting its recipient** — `add_contact` was pushed before every PKI
      direct message, and the firmware's `addFromContact()` sets `is_favorite` to sh

      …
  - version: v4.13.2-rc4
    name: v4.13.2-rc4
    datetime: '2026-07-25T19:47:27Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.2-rc4
    prerelease: true
    notes: >-
      Pre-release. Rolls up the complete `ok_to_mqtt` violation-detection feature and its pagination
      follow-up.


      ## Detect MQTT gateways that ignore `ok_to_mqtt`


      Meshtastic packets carry an `ok_to_mqtt` bit by which the originating node signals whether it
      consents to its traffic being uplinked to MQTT. Firmware only enforces it when a gateway
      relays *other* nodes' packets, and it skips the check entirely whenever the gateway believes
      its broker address is private — a plain IP-range test with no awareness of NAT or port
      forwarding. A gateway reaching its broker through a LAN-literal address that is *also*
      port-forwarded gets misclassified as private and silently relays opted-out traffic to what is
      effectively a public broker.


      MeshMonitor now detects that. When a received packet's bit is **explicitly clear** and the
      publishing gateway is not the originator, it is a provable violation — and it is recorded.


      **Important:** a violation is almost certainly a gateway **misconfiguration**, not malice.
      Treat it as something worth flagging to the gateway's operator.


      ### Where you see it


      - **Packet Monitor** — a `violation` badge on affected packets, and a per-gateway `ok_to_mqtt`
      column in the packet detail view showing which gateway violated (a packet can be relayed by
      many). Four states are distinguished: violation, allowed, opted out, and unknown (bit
      unreadable).

      - **Analysis & Reports → ok_to_mqtt Violations** — a cross-source report with a per-gateway
      summary (violation count, distinct originators affected, sources, first/last seen), drill-down
      to individual violating packets, window presets (24 h / 7 / 30 / 90 days) or an explicit date
      range, CSV export, and an optional "include unproven" toggle.


      ### Behaviour worth knowing


      - **Recording is on by default.** Violation history is kept ~90 days in its own store,
      independent of the MQTT Packet Monitor's much shorter window — so the report works out of the
      box. The per-packet **badge**, however, reads the Packet Monitor's capture log, which is
      **opt-in and off by default**; the Packet Monitor now says so explicitly when capture is off,
      so an absence of badges is never mistaken for an absence of violations.

      - **Forward-only.** Detection covers traffic received after upgrading. No backfill is possible
      — the bit was never previously stored — so expect an empty history at first.

      - **Confirmed vs. suspected.** Rows whose bit could not be read (undecryptable, or captured
      before this feature existed) are reported as *unknown/suspected*, never as confirmed
      violations, and they come from a much shorter retention window.


      ## Included changes


      - #4324 — Phase 1: backend detection, migration 128, durable history, cross-source endpoints

      - #4328 — Phase 2: Packet Monitor badge + per-gateway attribution

      - #4331 — Phase 3: the `ok_to_mqtt Violations` report

      - #4332 — fixes #4330: SQL sorting/paging so large result sets page correctly and ascending
      sorts are accurate


      ## Upgrade notes


      Migration **128** adds columns to `mqtt_packet_log` and creates the violation-history table.
      It is idempotent and was verified against SQLite, PostgreSQL 16 and MySQL 8.4. The new table
      is included in `migrate-db`, so backend-to-backend migrations carry violation history across.


      No configuration is required.

      ## 🚀 MeshMonitor v4.13.2-rc4


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.2-rc4
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.13.2-rc3
    name: v4.13.2-rc3
    datetime: '2026-07-24T13:14:34Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.2-rc3
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.2-rc3


      > ⚠️ **Pre-release** — this is a release candidate for testing. Docker images are not tagged
      `:latest`.


      This release candidate lands two major feature epics plus targeted fixes. The ATAK/CoT
      integration epic (#3691) arrives in full: TAKPacket V1 decoding with Packet Monitor previews
      and GeoChat messages, per-source ATAK contacts with a dedicated map layer, and a CoT TCP feed
      that ATAK/WinTAK clients can connect to directly. The TX-disabled epic (#4294/#4308) makes
      MeshMonitor a proper citizen on receive-only (listen-only) nodes: `lora.txEnabled=false` is
      now honored by a central server-side TX guard, transmit-dependent UI controls are gated,
      automations get a TX-disabled badge, and configuration import no longer silently forces TX
      back on. MQTT ingest now applies auto-delete-by-distance inline at ingest time rather than
      waiting for the periodic sweep (#3900). A touch-device layout bug that visually detached a
      sender's long name from its message bubble is fixed (#4311).


      ## Features

      - #4307 feat(atak): TAKPacket V1 decode, Packet Monitor previews, GeoChat messages (#3691
      Phase 1)

      - #4310 feat(atak): per-source ATAK contacts + map layer (#3691 Phase 2)

      - #4314 feat(atak): CoT TCP feed for ATAK/WinTAK clients (#3691 Phase 3)

      - #4309 feat(server): honor lora.txEnabled with central TX-disabled guard (#4294)

      - #4313 feat(ui): gate transmit-dependent controls when TX is disabled (#4294)

      - #4316 docs+feat: TX-disabled polish — automations badge, API docs, receive-only guide
      (#4294)

      - #4306 feat(mqtt): apply auto-delete-by-distance inline at ingest (#3900)


      ## Bug Fixes

      - #4312 fix(channels): tie long name to its bubble on touch devices (#4311)


      ## Chores

      - #4318 chore: bump version to 4.13.2-rc3


      ## Issues Resolved

      - #3691 Feature: ATAK / CoT integration for Meshtastic ATAK plugin packets

      - #4294 [BUG] Local Configuration tab silently forces LoRa txEnabled=true, breaking
      intentional TX-off (listen-only) nodes

      - #4308 Epic: Properly support LoRa tx_enabled=false (receive-only mode)

      - #3900 [FEAT] Apply auto-delete-by-distance inline at MQTT ingest, not just on periodic
      interval

      - #4311 [BUG] Long name visually detached from its message bubble on touch devices


      **Full Changelog**: https://github.com/Yeraze/meshmonitor/compare/v4.13.2-rc2...v4.13.2-rc3


      ## 🚀 MeshMonitor v4.13.2-rc3


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.2-rc3
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅ TypeScript checks passed

      ✅ Docker images built for linux/amd64, linux/arm64, linux/arm/v7


      ### 📋 Changes

      See commit history for detailed changes.
  - version: v4.13.2-rc2
    name: v4.13.2-rc2
    datetime: '2026-07-23T16:02:12Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.2-rc2
    prerelease: true
    notes: >-
      # MeshMonitor v4.13.2-rc2


      > ⚠️ **Pre-release (Release Candidate).** This build is for testing ahead of the v4.13.2
      stable release. Docker `:latest` tags are **not** updated. Report any issues you hit.


      This release candidate closes out the large multi-phase remediation epic (#3962) and the
      server-route extraction effort (#3502), decomposing the monolithic `server.ts` into focused
      route and service modules, migrating the frontend onto real routes + TanStack query hooks, and
      driving raw `fetch()` usage to zero. On the feature side it adds cross-source **NodeInfo
      Enrichment** (analysis report + apply API, #3837), a Portainer Stack export format in the
      configurator (#3724), richer MeshCore route popups (packet-flow mini map,
      `{ROUTE_NAMES}`/`{HASH_SIZE}` tokens, accessible detail modals), a per-trigger pre-send delay
      for the MeshCore Auto-Responder (#3953), configurable Auto-Ack DM resend attempts (#4266), and
      a map distinction between `ROUTER_LATE` and `ROUTER` with an unmessageable badge (#4295).
      Security fixes suppress a false-positive duplicate-key warning on the Meshtastic 2.8 NodeNum
      renumber (#4251) and add a self-service **Clear** action for stale security-risk warnings
      (#4302). CI hardening pins all GitHub Actions to commit SHAs. This is a testing build ahead of
      v4.13.2 stable.


      ## ✨ Features


      - **NodeInfo Enrichment** — cross-source analysis + apply API (#4287) and a report under
      Analysis & Reports (#4289) — resolves #3837

      - **Portainer Stack** export format in the configurator (#4282) — resolves #3724

      - **Map:** distinguish `ROUTER_LATE` from `ROUTER`, add unmessageable badge (#4303) — resolves
      #4295

      - **MeshCore:** `{ROUTE_NAMES}` / `{HASH_SIZE}` tokens + clickable route-detail popup (#4276)

      - **MeshCore:** packet-flow mini map in route popup + accessible detail modals (#4277)

      - **MeshCore:** per-trigger pre-send delay for the Auto-Responder (#4281) — resolves #3953

      - **Automation:** configurable Auto-Ack DM resend attempts (#4273) — resolves #4266


      ## 🐛 Bug Fixes & Security


      - **Security:** self-service **Clear** action for stale security-risk warnings (#4304) —
      resolves #4302

      - **Security:** suppress false-positive duplicate-key warning for the Meshtastic 2.8 NodeNum
      renumber (#4272) — resolves #4251


      ## ♻️ Refactoring


      **server.ts route extraction (#3502):**

      - Extract settings + admin routes (#4263)

      - Extract nodes routes + composition-root teardown (#4265)


      **Remediation epic — finish the half-migrated abstractions (#3962):**

      - 4.2a — retire `meshcoreRegistry` shim + `meshtasticManager` Proxy alias, and extract
      services: MQTT bridge/NodeDB, announce scheduler, admin-ack + favorites, device/remote-admin
      (#4267, #4268, #4269, #4270, #4271)

      - 4.2b — explicit connection state machine for `meshtasticManager` (#4274)

      - 4.3 — split `meshcoreRoutes` into concern modules (#4275)

      - 5.1–5.2 — memoize context providers + TanStack version-check (#4280)

      - 5.3 — SettingsTab draft-object rewrite (#4284)

      - 5.4 — router scaffold + full tab-to-route migration (#4285, #4286, #4288, #4290, #4291,
      #4292, #4293, #4296)

      - 5.5 — migrate raw `fetch()` sites to `ApiService`, baseline to zero (#4297, #4298)

      - 5.6 — `nodes.css` cascade-order fix + CSS containment convention (#4299)


      ## 🔧 CI / DevOps


      - Pin all GitHub Actions to commit SHAs (#4283)


      ## 📝 Docs


      - Epic #3962 Phase 4+5 close-out (#4300)

      - Add MeshEC to the Site Gallery (#4279)

      - Record the two verification gaps from the session (#4264)


      ## ✅ Issues Resolved


      #3502, #3724, #3837, #3953, #3962, #4251, #4266, #4278, #4295, #4302


      ## 🙏 New Contributors


      - @mirkosalvato1-ctrl — first contribution in #4283 (pinning Actions to SHAs). Welcome!


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.13.2-rc1...v4.13.2-rc2


      ## 🚀 MeshMonitor v4.13.2-rc2


      ### 📦 Installation


      **Docker (recommended):**

      ```bash

      docker run -d \
        --name meshmonitor \
        -p 8080:3001 \
        -v meshmonitor-data:/data \
        ghcr.io/Yeraze/meshmonitor:4.13.2-rc2
      ```


      ### 🧪 Testing

      ✅ All tests passed

      ✅

      …
  - version: v4.13.2-rc1
    name: v4.13.2-rc1
    datetime: '2026-07-21T20:59:22Z'
    url: https://github.com/Yeraze/meshmonitor/releases/tag/v4.13.2-rc1
    prerelease: true
    notes: >-
      > ⚠️ **Pre-release (Release Candidate 1 for 4.13.2).** Not marked `latest`; no `:latest`
      Docker tags are published. For testing ahead of the 4.13.2 release — please report issues.


      ## Summary


      Release candidate 1 for **4.13.2**. The headline is the completion of the **3D Map &
      Elevation** effort (issue #3826): Map Analysis gains a real 2D/3D toggle backed by a
      standalone MapLibre GL terrain view, a DEM tile proxy that hides the configured elevation
      source, and neighbor-link/traceroute layers with full selection parity in 3D (#4235, #4239,
      #4242). The Link Profile tool now seeds antenna heights from node-reported altitude, and
      neighbor links are finally clickable with terrain analysis available on route segments (#4253,
      #4254). Unified Messages shows sender short-names alongside long names, emoji reactions show
      the reactor's short name, and Copy NodeInfo gains per-field selection with a MAC-address fix
      (#4257, #4247, #4249). A security fix lets Bearer API tokens work on the legacy `/api/*`
      endpoints instead of only `/api/v1/*` (#4262, closes #4259). This RC also lands a
      PostgreSQL/MySQL migration ledger that stops migrations replaying on every boot (#4236), a
      batch of MeshCore hardware system-tests (#4232, #4241, #4248), and a round of follow-up fixes
      to the UI icon migration and map-visibility handling. Server routing was refactored out of the
      monolithic `server.ts` as the first step of a larger cleanup (#4260).


      ## Features


      - 3D Map & Elevation epic (#3826): neighbor-link terrain integration (#4235), 3D terrain view
      + DEM tile proxy (#4239), 3D neighbor/traceroute layers + selection parity (#4242)

      - Link Profile antenna AGL seeded from node-reported altitude (#4254)

      - Unified Messages: show short name alongside long name on sender labels (#4257)

      - Channels: show reactor's short name next to emoji reactions (#4247)

      - Per-field Copy NodeInfo with overwrite + MAC address display fix (#4249)

      - Unified Messages: reply box shows "Unknown message" when the parent is missing (#4246)

      - Consistent Lucide/emoji icon style across the full UI (#4217, @wilhel1812)


      ## Bug Fixes


      - Clickable neighbor links + terrain analysis on route segments (#4253)

      - Restore compact glyphs in the channel dropdown options after the icon migration (#4258)

      - Repair text spacing left behind by the icon migration (#4238)

      - Close the status-glyph gap in the no-hardcoded-ui-glyph rule (#4255)

      - Per-transport last-seen timestamps with decay + unstick visibility toggles (#4250, #4240)

      - Stop discarding MeshCore favorites in the node projection (#4256)

      - Fix MeshCore marker keying to prevent ghost markers across sources (#4237)

      - PostgreSQL/MySQL migration ledger — stop replaying migrations (and rebuilding
      `route_segments`) on every boot (#4236)


      ## Security


      - Accept Bearer API tokens on legacy `/api/*` endpoints, not just `/api/v1/*` (#4262, closes
      #4259)


      ## Refactoring


      - Extract messages/poll/config/user routes from `server.ts` (#4260, first step of #3502)


      ## Tests / CI


      - MeshCore hardware system-tests — Phase 1 connect/handshake (#4232), Phase 2 companion
      channel + repeater DM auto-ack (#4241), Phase 3 repeater remote-admin login + telemetry poll
      (#4248)


      ## Chore


      - Version bump to 4.13.2-rc1 (#4261)


      ## Issues Resolved


      - #3826 — 3D map mode for elevation display

      - #4259 — Bearer API tokens silently rejected on legacy endpoints

      - #4245 — Unified Messages "Unknown message" reply indicator

      - #4244 — Copy NodeInfo per-field selection + MAC address

      - #4243 — reactor short name on reactions

      - #4240 — favorited/freshly-heard node missing from map + stuck hide-from-map toggle

      - #4234 — MeshCore nodes visible on every map

      - #4233 — PostgreSQL/MySQL migrations replaying every boot

      - #4215 — expand Lucide/emoji icon style across the UI

      - #4193 — short name alongside long name for message senders


      **Full changelog:** https://github.com/Yeraze/meshmonitor/compare/v4.13.1...v4.13.2-rc1


      ## 🚀 MeshMonitor v4.13.2-rc1


      ### 📦 Installation


      **Docker (recommended):**

      ```

      …
